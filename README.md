# [![DeloPay](https://delopay.net/logo-lockup.svg)](https://delopay.net)

## DeloPay Status

This repository powers [**status.delopay.net**](https://status.delopay.net) — the
public status page for the DeloPay payment orchestration platform. It is built on
[Upptime](https://upptime.js.org): a status page powered entirely by GitHub
Actions, Issues, and Pages. There is no server to maintain — scheduled workflows
ping each endpoint every 5 minutes, record the response, and update this page
automatically.

[![Uptime CI](https://github.com/DeloPay-net/delopay-status/workflows/Uptime%20CI/badge.svg)](https://github.com/DeloPay-net/delopay-status/actions/workflows/uptime.yml)
[![Response Time CI](https://github.com/DeloPay-net/delopay-status/workflows/Response%20Time%20CI/badge.svg)](https://github.com/DeloPay-net/delopay-status/actions/workflows/response-time.yml)
[![Graphs CI](https://github.com/DeloPay-net/delopay-status/workflows/Graphs%20CI/badge.svg)](https://github.com/DeloPay-net/delopay-status/actions/workflows/graphs.yml)
[![Static Site CI](https://github.com/DeloPay-net/delopay-status/workflows/Static%20Site%20CI/badge.svg)](https://github.com/DeloPay-net/delopay-status/actions/workflows/site.yml)
[![Summary CI](https://github.com/DeloPay-net/delopay-status/workflows/Summary%20CI/badge.svg)](https://github.com/DeloPay-net/delopay-status/actions/workflows/summary.yml)

## [📈 Live Status](https://status.delopay.net): <!--live status--> **🟩 All systems operational**

<!--start: status pages-->
<!-- This summary is generated by Upptime (https://github.com/upptime/upptime) -->
<!-- Do not edit this manually, your changes will be overwritten -->
<!-- prettier-ignore -->
| URL | Status | History | Response Time | Uptime |
| --- | ------ | ------- | ------------- | ------ |

<!--end: status pages-->

> The table above is generated automatically once the workflows have run for the
> first time. Until then it stays empty.

## Monitored services

**Production**

| Service | Endpoint |
| --- | --- |
| API | `https://api.delopay.net/health` |
| Merchant Dashboard | `https://dashboard.delopay.net` |
| Checkout | `https://checkout.delopay.net` |
| Website | `https://delopay.net` |
| Documentation | `https://delopay.net/docs/` |

**Sandbox**

| Service | Endpoint |
| --- | --- |
| Sandbox API | `https://sandbox-api.delopay.net/health` |
| Sandbox Dashboard | `https://sandbox.delopay.net` |
| Sandbox Admin | `https://admin-sandbox.delopay.net` |

## Adding or changing a monitor

All configuration lives in [`.upptimerc.yml`](./.upptimerc.yml). To monitor a new
service, add an entry to the `sites` list and commit to the default branch — the
workflows pick up the change on the next run. See the
[Upptime configuration docs](https://upptime.js.org/docs/configuration) for the
full set of options (custom headers, expected status codes, content assertions,
notifications, and more).

## How it works

- **Uptime CI** runs every 5 minutes and pings each endpoint. If a check fails, it
  opens a GitHub Issue as an incident and closes it automatically when the service
  recovers.
- **Response Time CI** records latency and commits it to git, building a historical
  trend over time.
- **Graphs CI** renders the response-time charts shown on the status page.
- **Static Site CI** builds the Svelte status page and deploys it to GitHub Pages.
- **Summary CI** keeps this README's status table in sync.

## Setup checklist (one-time)

These steps are done in the GitHub repository settings, not in code:

1. **GitHub Pages** — Settings → Pages → deploy from the `gh-pages` branch (created
   automatically by the Static Site CI on its first run).
2. **Custom domain** — add a DNS `CNAME` record `status` → `delopay-net.github.io`,
   so the page resolves at `status.delopay.net` (already set as `cname` in the config).
3. **`GH_PAT` secret** *(recommended)* — a fine-grained personal access token with
   `contents`, `issues`, and `pages` write access, stored as the `GH_PAT` Actions
   secret. The workflows fall back to the default `GITHUB_TOKEN` if it is absent,
   but a dedicated PAT avoids rate limits and lets incident issues be labelled and
   assigned correctly.

## License

The status-page tooling is provided by [Upptime](https://github.com/upptime/upptime)
under the [MIT License](https://github.com/upptime/upptime/blob/master/LICENSE).
DeloPay-specific configuration and content in this repository is © Deloxity LLC.
